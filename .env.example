# Base URL of the backend API (no trailing slash).
API_BASE_URL=http://stg-api.runasp.net/api

# Absolute URL this app is served from (no trailing slash). Used for
# redirect targets; optional in development.
APP_BASE_URL=http://localhost:3000
